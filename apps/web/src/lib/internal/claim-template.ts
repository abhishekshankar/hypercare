/** Prefilled on brief claim; writers replace with real copy (TASK-028). */
export const DEFAULT_CLAIMED_MODULE_BODY = `## Opening

Short orientation (replace with real content from the brief).

## What to try

Add step-by-step content. Use \`##\` headings; each section becomes a retrievable chunk.

## Limits

This module is educational only — not medical or legal advice. Replace attribution in module settings before publish.
`;
