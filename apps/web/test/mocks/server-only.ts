/**
 * Stubs the `server-only` package in Vitest (see vitest.config.ts `resolve.alias`).
 * Next.js enforces the real `server-only` at build time; unit tests are server-only by convention.
 */
export {};
