import { describe, expect, it } from "vitest";

describe("privacy export API", () => {
  it.skip("POST → S3 presign (requires S3 and DATABASE_URL in CI)", () => {
    expect(true).toBe(true);
  });
});
