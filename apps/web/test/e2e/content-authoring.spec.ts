import { test } from "@playwright/test";

test.skip("content lead walks /internal/content (requires seeded role + login)", async () => {
  // Implement when E2E auth can grant `content_lead` (see TASK-028 PM verify).
});
